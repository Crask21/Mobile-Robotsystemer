from signalProcessing.Threadprog import LISTEN
from Send_DTMF.Class_DTMF import SEND

class DTMF:

    # DTMF Settings
    fs = 44100
    amplitude = 5000
    media = 'PyGame' # 'SD'
    fade_P = 0.02
    baud_rate = 10

